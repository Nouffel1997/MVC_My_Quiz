{% extends 'base.html.twig' %}

{% block title %}Questions{% endblock %}

{% block body %}

 <ul>
        {% for post in posts %}
        <li>{{ post.question }}</li>
        
        {% endfor %}
{% for bob in bob %}
        <li>{{ bob.reponse }}</li>
        <a href="{{ path('reponse_expected') }}">Question</a><br/>
        {% endfor %}

        {% for i in 1..((totalPosts/1)|round(0, 'ceil')) %}
        <a href="{{ path('bob', {page: i}) }}">{{ i }}</a>
        {% endfor %}
      
    </ul>

{% endblock %}