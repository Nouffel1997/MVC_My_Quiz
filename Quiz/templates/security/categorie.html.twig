{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}

 <ul>
        {% for post in posts %}
        <li>{{ post.name }}</li>
       <a href="/categorie/edit/{{ post.id }}" class="btn btn-dark">Modifier</a>
       <a href="/categorie/delete/{{ post.id }}" class="btn btn-dark">Supprimer</a>
       

        {% endfor %}
       <a href="{{ path('new_categorie') }}" class="btn btn-primary btn-lg btn-block" role="button" aria-pressed="true">Créer une
    Categorie</a>
    <a href="{{ path('bob') }}">Quiz Harry Potter</a><br/>
    <a href="{{ path('sigle') }}">Quiz Sigle France</a><br/>
    <a href="{{ path('definition') }}">Quiz Definition</a><br/>
    
    
    </ul>

{% endblock %}